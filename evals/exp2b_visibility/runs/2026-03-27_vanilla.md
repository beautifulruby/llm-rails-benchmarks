# Exp 2B Visibility: Vanilla Run

**Timestamp:** 2026-03-27
**Agent:** Claude sub-agent via Task tool
**App:** vanilla/

## Files Read (10)

1. app/models/comment.rb
2. app/models/user.rb
3. app/controllers/comments_controller.rb
4. test/integration/comment_visibility_test.rb
5. app/controllers/posts_controller.rb
6. app/controllers/application_controller.rb
7. app/views/posts/show.html.erb
8. app/views/posts/_comment.html.erb
9. test/models/comment_test.rb
10. test/controllers/comments_controller_test.rb

## Files Modified (5)

1. app/models/comment.rb
2. app/controllers/posts_controller.rb
3. app/views/posts/_comment.html.erb
4. test/models/comment_test.rb
5. test/integration/comment_visibility_test.rb

## Diff Stats

```
5 files changed, 163 insertions(+), 17 deletions(-)
```

## Implementation Summary

### Model Changes (comment.rb)
```ruby
def visible_to?(user)
  return true if approved?
  return true if user&.admin?
  return true if pending? && user == self.user
  false
end

def self.visible_to(user)
  if user&.admin?
    all
  else
    where(moderation_status: "approved")
      .or(where(moderation_status: "pending", user_id: user&.id))
  end
end
```

### Controller Changes
Replaced conditional logic with scope: `@comments = @post.comments.root_comments.visible_to(current_user)`

### View Changes
Updated partial: `visible_replies = comment.replies.visible_to(current_user)`

### Tests Added
- 8 model tests for `visible_to?` and `visible_to` scope
- 4 integration tests for visibility rules

## Commit

```
f039f56 Exp 2B (Vanilla): Author can see their own pending comments
```
