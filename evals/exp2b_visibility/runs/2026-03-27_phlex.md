# Exp 2B Visibility: Phlex Run

**Timestamp:** 2026-03-27
**Agent:** Claude sub-agent via Task tool
**App:** phlex/

## Files Read (9)

1. app/models/comment.rb
2. app/models/user.rb
3. app/controllers/comments_controller.rb
4. app/controllers/posts_controller.rb
5. test/models/comment_test.rb
6. test/controllers/comments_controller_test.rb
7. test/integration/comments_integration_test.rb
8. test/fixtures/comments.yml
9. test/fixtures/users.yml

## Files Modified (4)

1. app/models/comment.rb
2. app/controllers/posts_controller.rb (includes inline views)
3. test/models/comment_test.rb
4. test/integration/comments_integration_test.rb

## Diff Stats

```
4 files changed, 102 insertions(+), 11 deletions(-)
```

## Implementation Summary

### Model Changes (comment.rb)
```ruby
def visible_to?(user, is_admin:)
  return true if is_admin
  return true if status == "approved"
  return true if status == "pending" && user && user.id == user_id
  false
end

def self.visible_to(user, is_admin:)
  if is_admin
    all
  elsif user
    where("status = ? OR (status = ? AND user_id = ?)", "approved", "pending", user.id)
  else
    approved
  end
end
```

### Controller/View Changes (posts_controller.rb)
All in one file:
- Updated `show` action to use `visible_to` scope
- Updated `CommentView#view_template` to use `visible_to?`
- Updated replies section to filter with `visible_to`

### Tests Added
- 6 model tests for visibility methods
- 1 integration test for author visibility

## Key Difference from Vanilla

The view changes and controller changes were in the **same file**. Agent didn't need to open a separate view partial. This contributed to fewer files modified and less total code.

## Commit

```
dcba1b3 Exp 2B (Phlex): Author can see their own pending comments
```
